/**
 * Host-build stub for STM32H7 device header.
 *
 * The real CMSIS device header is provided by the ARM toolchain for
 * firmware builds. Unit tests compile driver sources on the host, where
 * only the standard integer types are needed.
 */

#ifndef STM32H7XX_HOST_STUB_H
#define STM32H7XX_HOST_STUB_H

#include <stdint.h>

#endif /* STM32H7XX_HOST_STUB_H */
